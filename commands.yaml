# GENERIC COMMANDS

generic_commands:
   commands: |
           show network-instance default protocol BGP default all-neighbor * afi-safi l2vpn-evpn | select state session-state | display json
           show network-instance default protocol BGP default rib afi-safi L2VPN_EVPN loc-rib route route-type 2 | display json
           show network-instance default protocol BGP default rib afi-safi L2VPN_EVPN loc-rib route route-type 3 | display json
           show network-instance default protocol BGP default rib afi-safi L2VPN_EVPN loc-rib route route-type 5 | display json
   tags: arcos

# Do not remove this three dashes (“---”) they are used to separate documents
---
